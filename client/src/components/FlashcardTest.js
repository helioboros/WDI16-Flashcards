import React, {Component} from "react"

class FlashcardTest extends Component {
    //component will mount uuh
    //make a copy array of the flashcards and just store the original
    //like on page loading make this array
    //function to bring up a random flashcard
    //one that you can call back to later
    //function to delete a flashcard from the temporary array
    //function to skip the flashcard and put it back into the array ??
    render() {
        return (
            <div>testy</div>
        )
    }
}

export default FlashcardTest