import React, {Component} from "react"

//some kind of function to display a random flashcard from a temporary array made
//if answer is right remove it from the array
//if answer is wrong or if user skips then roll again

class FlashcardPage extends Component {
    render() {
        return (
            <div>hungus bungus</div>
        )
    }
}

export default FlashcardPage